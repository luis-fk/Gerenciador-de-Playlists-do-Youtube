package com.redplay.redplay.site;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe main que inicia o SpringBoot e seu Controller (RedPlayController).
 */
@SpringBootApplication
public class RedPlayApplication {

	/**
	 * Método main que inicia o SpringBoot e seu Controller (RedPlayController).
	 * @param args String de argumento.
	 */
	public static void main(String[] args) {
		SpringApplication.run(RedPlayApplication.class, args);
	}
}
